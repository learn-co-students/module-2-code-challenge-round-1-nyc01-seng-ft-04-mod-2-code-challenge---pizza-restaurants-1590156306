<% if flash[:errors] %>
    <% flash[:errors].each do |error| %>
        <p><%= error %></p>
    <% end %>
<% end %>

<%= form_for @restaurantpizza do |f| %>
    <%= f.label :price %>
    <%= f.number_field :price %>

    <%= f.label :pizza_id %>
    <%= f.collection_select :pizza_id, @pizzas, :id, :name %>

    <%= f.label :restaurant_id %>
    <%= f.collection_select :restaurant_id, @restaurants, :id, :name %>

    <%= f.submit %>

<% end %>