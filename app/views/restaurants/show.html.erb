<h1><%= @restaurant.name %> </h1>

<p><%= @restaurant.address %> </p>

<ul> Pizzas:
    <% @restaurant.pizzas.each do |pizza| %>
    <li><%=link_to pizza.name, pizza_path(pizza) %>
        <% prp = pizza.restaurant_pizzas.find_by(restaurant_id: @restaurant.id) %>
        <% rp = RestaurantPizza.find_by(restaurant_id: @restaurant.id, pizza_id: prp.pizza_id) %>
        <%= rp.price %>
    </li>
    <% end %>
</ul>

<p>Average Pizza Price: 
    <%= @restaurant.average_price %>
</p>